{
  "frontend/src/App.tsx": "Main application component with QueryClientProvider wrapping for React Query support, five-tab navigation, and full app reset functionality integrated with error boundary.",
  "frontend/src/state/appState.tsx": "React context provider managing global app state with support for both single and multi-value VLOOKUP results, inline workbook editing via updateWorkbookCell and updateWorkbookRow helpers, and backend history persistence.",
  "frontend/src/lib/excel/parseWorkbook.ts": "Client-side Excel file parser with hardened SheetJS CDN loading (timeout, duplicate script detection, single in-flight promise) and comprehensive validation to ensure workbooks have valid sheets and sheet data before returning, with proper TypeScript null checks to prevent type errors.",
  "frontend/src/lib/vlookup/vlookup.ts": "VLOOKUP implementation supporting both single and multi-value lookups with exact and approximate match types, returning full row data for matched results to enable inline editing.",
  "frontend/src/components/layout/AppLayout.tsx": "Main layout wrapper providing consistent structure with header, main content area, and footer across all pages.",
  "frontend/src/components/layout/AppHeader.tsx": "Application header with responsive layout (flex-col on mobile, flex-row on desktop) displaying an enlarged logo (h-12 on mobile, h-16 on desktop) with Replace Excel and Reset buttons that prevent concurrent replaces, validate parsed workbooks, and surface errors via global state.",
  "frontend/src/pages/UploadTab.tsx": "Upload page with file input, sheet selection, data preview table, and Replace Excel button; empty state displays the new round red PATENTED logo with combined flip and continuous rotation animation (20s duration, disabled for reduced-motion users), centered and slightly smaller (max-w-xs).",
  "frontend/src/pages/SearchTab.tsx": "Search configuration page with VLOOKUP (supporting multi-value comma/newline separated input via Textarea) and Filter Data modes, clearing Update Checking results when running VLOOKUP searches.",
  "frontend/src/pages/ResultsTab.tsx": "Results display page with responsive layout (flex-col/flex-row breakpoints) showing either Update Checking new items or multi-value VLOOKUP results with inline row editing, full workbook Excel export, PDF export of matched rows only, proper text wrapping in tables (break-words, max-w-xs), and horizontal scroll support for wide tables.",
  "frontend/src/components/table/DataPreviewTable.tsx": "Reusable table component for displaying sheet data with headers, row numbers, and scrollable content area.",
  "frontend/src/index.css": "Global styles with blue ocean color palette using OKLCH color system featuring deep blues (hue 220), teals (hue 195), and ocean-inspired colors for both light and dark modes.",
  "frontend/index.html": "HTML entry point with Crystal Atlas branding in title and favicon reference to the uploaded logo.",
  "frontend/package.json": "Package configuration with all dependencies except xlsx removed since we're loading it dynamically from CDN to avoid TypeScript type issues.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for backend History operations (list, add, clear) with conversion utilities between frontend and backend data formats, including graceful error handling and local fallback support; HistoryEntry id field is now optional to support new entries without IDs.",
  "frontend/tailwind.config.js": "Tailwind configuration with extended ocean-themed design tokens including custom shadow effects, wave animation keyframes, and new flip-and-rotate animation (20s linear infinite) combining horizontal flip with continuous 360-degree rotation for the Upload tab logo.",
  "frontend/src/components/vlookup/VlookupAnimation.tsx": "Full-screen animated visualization component that demonstrates the VLOOKUP process step-by-step with cell highlighting, scanning animation, and playback controls (play/pause/reset).",
  "frontend/src/pages/UpdateCheckingTab.tsx": "Update checking page that compares old and new Excel files using key-presence comparison to identify new items, with key column selection and navigation to results on completion.",
  "frontend/src/lib/export/branding.ts": "Shared branding utilities with robust error handling for loading Crystal Atlas logo and tablet-logo watermark as Image elements for PDF exports.",
  "frontend/src/lib/export/exportXlsx.ts": "Client-side Excel export utility that dynamically loads SheetJS library and generates branded .xlsx files with Crystal Atlas title header, supporting full workbook export with inline edits applied.",
  "frontend/src/lib/export/exportPdf.ts": "Professional PDF export utility with logo-left header, right-aligned contact details with text wrapping, comprehensive footer with wrapping support, tiled tablet-logo watermark pattern at ~30% opacity, automatic pagination with repeated headers, and dynamic row heights based on wrapped cell content to prevent text overlap.",
  "frontend/src/lib/compare/compareWorkbooks.ts": "Workbook comparison logic with three modes: key-presence mode (identifies keys present in New but not in Old), row mode (compares full-row data), and column mode (identifies specific column changes for matching keys).",
  "frontend/src/pages/HistoryTab.tsx": "History page with responsive layout (flex-col/flex-row breakpoints, text wrapping on narrow widths) that loads from backend via React Query with local fallback, displays VLOOKUP/filter/update-checking history in separate tabs with responsive tab labels (text-xs on mobile), exports only unmatched rows for update checking, and includes loading/error states with English-only messaging.",
  "frontend/src/components/errors/AppErrorBoundary.tsx": "React class-based error boundary component that catches render errors, window errors, and unhandled promise rejections, displaying a recovery UI with Reset Application and Reload Page buttons to prevent white screens.",
  "frontend/src/lib/excel/normalizeHeader.ts": "Shared utility for normalizing Excel header text (trimming whitespace) and finding header indices by normalized name, ensuring consistent header-based matching across files even when extra whitespace is present.",
  "frontend/src/lib/compare/filterComparisonRows.ts": "Null-safe filtering utility that searches comparison rows by keyword across key values and all cell values, supporting both exact and partial (contains) match types.",
  "frontend/src/components/updateChecking/UpdateCheckingResultsSearchBar.tsx": "Presentational search bar component for Update Checking results with keyword input, match type selector (Exact/Contains), and clear button.",
  "frontend/src/lib/compare/normalizeRowSignature.ts": "Utility for generating deterministic full-row signatures and aligning old rows to new sheet headers by name mapping, supporting null-safe comparison and header-order normalization for Update Checking.",
  "frontend/src/lib/export/pdfCompanyInfo.ts": "Centralized company information constants for PDF header contact block and footer info block, providing single source of truth for all PDF export branding text."
}