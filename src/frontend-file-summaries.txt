{
  "frontend/src/App.tsx": "Main application component with five-tab navigation (Upload, Search, Results, Update Checking, History) wrapped in AppErrorBoundary to catch any render-time errors and provide a recovery UI instead of a blank screen.",
  "frontend/src/state/appState.tsx": "React context provider managing global app state with enhanced normalizeWorkbook helper and ComparisonResult interface now including mode property ('row' or 'column') to support both row-level and column-level difference checking in update comparisons.",
  "frontend/src/lib/excel/parseWorkbook.ts": "Client-side Excel file parser with hardened SheetJS CDN loading (timeout, duplicate script detection, single in-flight promise) and comprehensive validation to ensure workbooks have valid sheets and sheet data before returning, with proper TypeScript null checks to prevent type errors.",
  "frontend/src/lib/vlookup/vlookup.ts": "VLOOKUP implementation supporting exact and approximate match types with comprehensive error handling and result messaging.",
  "frontend/src/components/layout/AppLayout.tsx": "Main layout wrapper providing consistent structure with header, main content area, and footer across all pages.",
  "frontend/src/components/layout/AppHeader.tsx": "Application header displaying Crystal Atlas logo and title with Replace Excel and Reset buttons that prevent concurrent replaces, validate parsed workbooks before state updates, and surface all errors via global state without throwing exceptions.",
  "frontend/src/pages/UploadTab.tsx": "Upload page with file input, sheet selection, data preview table, and Replace Excel button that validates parsed workbooks defensively, guards Select values against invalid selections, surfaces all errors via visible Alert components, and keeps the UI interactive even when errors occur.",
  "frontend/src/pages/SearchTab.tsx": "Search configuration page with two modes (VLOOKUP and Filter Data) that validates and resets column selections when workbook/sheet changes, guards all Select values to prevent invalid selections from triggering render-time errors, and includes export to Excel/PDF functionality for filtered results.",
  "frontend/src/pages/ResultsTab.tsx": "Results display page showing either VLOOKUP results or update checking comparison results with integrated keyword search functionality that filters displayed rows and exports, supporting both row and column comparison modes with mode-aware rendering and export functionality.",
  "frontend/src/components/table/DataPreviewTable.tsx": "Reusable table component for displaying sheet data with headers, row numbers, and scrollable content area.",
  "frontend/src/index.css": "Global styles with blue ocean color palette using OKLCH color system featuring deep blues (hue 220), teals (hue 195), and ocean-inspired colors for both light and dark modes.",
  "frontend/index.html": "HTML entry point with Crystal Atlas branding in title and favicon reference to the uploaded logo.",
  "frontend/package.json": "Package configuration with all dependencies except xlsx removed since we're loading it dynamically from CDN to avoid TypeScript type issues.",
  "frontend/src/hooks/useQueries.ts": "Empty placeholder file as app uses client-side processing without backend queries.",
  "frontend/tailwind.config.js": "Tailwind configuration with extended ocean-themed design tokens including custom shadow effects and wave animation keyframes.",
  "frontend/src/components/vlookup/VlookupAnimation.tsx": "Full-screen animated visualization component that demonstrates the VLOOKUP process step-by-step with cell highlighting, scanning animation, and playback controls (play/pause/reset).",
  "frontend/src/pages/UpdateCheckingTab.tsx": "Update checking page with comparison mode toggle (row vs column difference), normalized header-based key column selection supporting any column title, auto-selection of \"Patent No.\" as default when available, and comprehensive validation with clear error messages.",
  "frontend/src/lib/export/branding.ts": "Shared branding utilities providing Crystal Atlas title and logo loading functions for embedding in Excel and PDF exports.",
  "frontend/src/lib/export/exportXlsx.ts": "Client-side Excel export utility that dynamically loads SheetJS library and generates branded .xlsx files with Crystal Atlas title header.",
  "frontend/src/lib/export/exportPdf.ts": "Client-side PDF export utility that dynamically loads jsPDF library and generates branded PDF files with Crystal Atlas logo and title header, including addPage method in type definition.",
  "frontend/src/lib/compare/compareWorkbooks.ts": "Workbook comparison logic supporting two modes: row difference (identifies new rows only) and column difference (identifies which specific columns changed for matching keys), with normalized header-based matching and detailed change tracking.",
  "frontend/src/pages/HistoryTab.tsx": "History page displaying all past VLOOKUP searches, filter operations, and update checking comparisons with separate tabs for each type, showing only New Rows count for update checking history items and exporting only new rows to Excel/PDF.",
  "frontend/src/components/errors/AppErrorBoundary.tsx": "React error boundary component that catches both render-time errors and runtime errors (via global window 'error' and 'unhandledrejection' event listeners registered on mount and cleaned up on unmount) and displays a clear English fallback recovery UI with working Reset Application and Reload Page actions, preventing white screens when unexpected errors occur anywhere in the app.",
  "frontend/src/lib/excel/normalizeHeader.ts": "Shared utility for normalizing Excel header text (trimming whitespace) and finding header indices by normalized name, ensuring consistent header-based matching across files even when extra whitespace is present.",
  "frontend/src/lib/compare/filterComparisonRows.ts": "Null-safe filtering utility that searches comparison rows by keyword across key values and all cell values, supporting both exact and partial (contains) match types.",
  "frontend/src/components/updateChecking/UpdateCheckingResultsSearchBar.tsx": "Presentational search bar component for Update Checking results with keyword input, match type selector (Exact/Contains), and clear button."
}